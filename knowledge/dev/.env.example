# tenant-local (dev-only) — no real secrets, Postgres is thrown away with the volume.
POSTGRES_USER=kv
POSTGRES_PASSWORD=kv_local_dev
POSTGRES_DB=knowledge_vault
POSTGRES_PORT=55432

GATEWAY_PORT=8787
TENANT_ID=local-dev
# Origin of `npm run dev` in knowledge/ — must match exactly for CORS + cookies.
DEV_ORIGIN=http://localhost:5173
